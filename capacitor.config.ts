import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'unisuam-web2-prova2',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
